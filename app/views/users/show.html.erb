<%= provide(:title, @user.name) %>

<div class="static-pages">
  <br/>
  <%= render 'shared/flash' %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1>
          <%= gravatar_for @user %>
          <%= @user.name %>
        </h1>
      </section>
    </aside>
  </div>
</div>